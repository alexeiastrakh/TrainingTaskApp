<Page
    x:Class="TrainingTaskApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TrainingTaskApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20">
            <TextBox x:Name="FirstNameTextBox" PlaceholderText="Enter first name" Text="{Binding NewPerson.FirstName, Mode=TwoWay}"/>
            <TextBox x:Name="LastNameTextBox" PlaceholderText="Enter last name" Text="{Binding NewPerson.LastName, Mode=TwoWay}"/>
            <Button Content="Add Person" Command="{Binding AddCommand}"/>
            <ListView x:Name="PersonListView" ItemsSource="{Binding People}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBox Text="{Binding FirstName, Mode=TwoWay}" Margin="5" Grid.Column="0"/>
                            <TextBox Text="{Binding LastName, Mode=TwoWay}" Margin="5" Grid.Column="1"/>
                            <Button Content="Edit" Command="{Binding DataContext.EditCommand, ElementName=PersonListView}" CommandParameter="{Binding}" Grid.Column="2"/>
                            <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, ElementName=PersonListView}" CommandParameter="{Binding}" Grid.Column="3"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
